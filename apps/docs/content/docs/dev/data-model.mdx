---
title: 데이터 모델
description: 계정, 거래, 전기 — 그리고 units/weight 분리
type: reference
summary: posting마다 두 정수를 저장한다. units는 사실, weight는 측정.
related: ['/docs/domain/policy', '/docs/dev/system']
---

<SpecVersion
  spec="데이터 모델"
  version="migration 4"
  source="packages/store-sqlite/migrations/"
  history={[
    { version: '1', date: '2026-07-17', summary: '초기 스키마 — book, commodity, account, txn, posting, card, installment, recurring, audit_log', ref: 'packages/store-sqlite/migrations/20260717100240_init' },
    { version: '2', date: '2026-07-17', summary: '불변식 트리거 11개. drizzle-kit이 트리거를 모델링하지 않아 손으로 쓴 custom 마이그레이션', ref: 'packages/store-sqlite/migrations/20260717100308_invariant_triggers' },
    { version: '3', date: '2026-07-17', summary: 'loan + loan_schedule_row', ref: 'packages/store-sqlite/migrations/20260717101844_loans' },
    { version: '4', date: '2026-07-17', summary: 'ingest_batch + ingest_item', ref: 'packages/store-sqlite/migrations/20260717105405_ingest' },
  ]}
/>

버전은 사이트 공통이 아니라 **이 스펙만의 번호**다. 데이터 모델은 마이그레이션 4개를
거쳤고 CLI는 0.1.0이며 도메인 정책은 버전이 없다. 번호 하나를 셋에 같이 찍으면
함께 움직인다고 읽히지만, 실제로는 그렇지 않다.

이력도 지어낸 목록이 아니다. `__holiday_migrations`가 마이그레이션마다 sha256을
남기고, 적용분을 고치면 원장이 열리길 거부한다 —
[POLICY-016](/docs/domain/policy#policy-016).

## 무엇이 사실이고 무엇이 예측인가

아키텍처의 뼈대가 이 구분이다. 원장에는 일어난 일만, 스케줄에는 일어날 일만 둔다.
현금흐름은 둘에서 유도할 뿐 — 저장하지 않는다.

<Canvas
  height={420}
  nodes={[
    { id: 'txn', type: 'doc', position: { x: 0, y: 60 }, data: { label: 'txn + posting', description: '원장. append-only', kind: 'fact', handles: { target: false } } },
    { id: 'card', type: 'doc', position: { x: 0, y: 170 }, data: { label: 'card', description: '청구주기', kind: 'forecast', handles: { target: false } } },
    { id: 'inst', type: 'doc', position: { x: 0, y: 250 }, data: { label: 'installment', description: '할부 스케줄', kind: 'forecast', handles: { target: false } } },
    { id: 'rec', type: 'doc', position: { x: 0, y: 330 }, data: { label: 'recurring', description: '정기지출', kind: 'forecast', handles: { target: false } } },
    { id: 'loan', type: 'doc', position: { x: 0, y: 410 }, data: { label: 'loan', description: '상환 스케줄', kind: 'forecast', handles: { target: false } } },
    { id: 'balance', type: 'doc', position: { x: 300, y: 20 }, data: { label: 'balance', description: 'SUM(weight) — 과거', kind: 'derived' } },
    { id: 'cashflow', type: 'doc', position: { x: 300, y: 200 }, data: { label: 'cashflow', description: '과거 + 미래', kind: 'derived' } },
    { id: 'audit', type: 'doc', position: { x: 300, y: 110 }, data: { label: 'audit_log', description: '해시 체인', kind: 'fact' } },
  ]}
  edges={[
    { id: 'e1', source: 'txn', target: 'balance', animated: true },
    { id: 'e2', source: 'txn', target: 'audit' },
    { id: 'e3', source: 'txn', target: 'cashflow', animated: true },
    { id: 'e4', source: 'card', target: 'cashflow' },
    { id: 'e5', source: 'inst', target: 'cashflow' },
    { id: 'e6', source: 'rec', target: 'cashflow' },
    { id: 'e7', source: 'loan', target: 'cashflow' },
  ]}
/>

<CanvasLegend />

스케줄에서 원장으로 가는 화살표가 **없다**. 예측은 전기되지 않는다 — 금리 변경이나
구독 해지마다 과거가 오염되는 길을 막기 위해서다.

## posting

<SchemaTable
  name="posting"
  kind="table"
  source="packages/store-sqlite/src/schema.drizzle.ts"
  description="한 거래의 한 다리. 두 정수가 핵심이다."
  fields={[
    { name: 'txn_id', type: 'TEXT', required: true, description: 'ULID' },
    { name: 'seq', type: 'INTEGER', required: true, unit: '0-based', description: '정렬은 이걸로만 — 사용자 문자열로 정렬하면 기계마다 해시가 달라진다' },
    { name: 'account_id', type: 'TEXT', required: true },
    { name: 'units_minor', type: 'INTEGER', required: true, unit: 'minor units of `commodity`', invariant: 'i64', description: '사실 — 무엇이 움직였나' },
    { name: 'commodity', type: 'TEXT', required: true, invariant: 'account.commodity와 일치', description: '트리거가 강제. $를 ₩로 오독하는 걸 잡는 링' },
    { name: 'weight_minor', type: 'INTEGER', required: true, unit: 'minor units of KRW', invariant: 'SUM = 0 per txn', description: '측정 — 같은 사건의 KRW 값' },
    { name: 'weight_source', type: 'TEXT', required: true, invariant: "identity|actual|rate|plug", description: '출처. rate면 잠정치' },
    { name: 'fx_rate_text', type: 'TEXT', unit: 'decimal string', description: '감사용. 절대 균형의 근거가 아님' },
    { name: 'lot_id', type: 'TEXT', description: '원가 basis용 seam. 균형은 절대 참조 안 함' },
  ]}
/>

## account

<SchemaTable
  name="account"
  kind="table"
  source="packages/store-sqlite/src/schema.drizzle.ts"
  description="코드가 곧 경로다. 서브트리 조회는 code = ? OR code GLOB ? || ':*'."
  fields={[
    { name: 'id', type: 'TEXT', required: true, description: 'ULID. posting은 code가 아니라 이걸 참조한다 — 그래서 계정 이름을 바꿔도 과거가 안 바뀐다' },
    { name: 'code', type: 'TEXT', required: true, unit: 'materialized path', invariant: 'UNIQUE', description: 'Assets:Bank:KB:Checking' },
    { name: 'type', type: 'TEXT', required: true, invariant: "asset|liability|equity|income|expense" },
    { name: 'commodity', type: 'TEXT', invariant: 'posting과 일치 (트리거)', description: 'NULL이면 다중 통화 opt-in. 기본은 단일이고, $를 ₩로 오독하는 걸 잡는 링' },
    { name: 'cash', type: 'INTEGER', required: true, unit: '0|1', description: 'cashflow가 잔고를 걸어가는 출발점. 접두어 규칙이 아니라 플래그 — ADR-004' },
    { name: 'monetary', type: 'INTEGER', required: true, unit: '0|1', description: 'IAS 21. 화폐성 항목만 마감 때 재평가. 타입에서 추론 불가라 플래그' },
    { name: 'placeholder', type: 'INTEGER', required: true, unit: '0|1', invariant: '1이면 기표 불가 (트리거)' },
  ]}
/>

## 스케줄 테이블

넷 모두 부채 계정에 붙는 **예측**이며 원장 밖에서 산다. 상세는
[Features](/docs/features/cashflow)에 있다.

<SchemaTable
  name="loan"
  kind="table"
  source="packages/store-sqlite/src/schema.drizzle.ts"
  description="상환 스케줄. loan_schedule_row가 회차를 담는다."
  fields={[
    { name: 'account_id', type: 'TEXT', required: true, invariant: 'type = liability', description: 'Liabilities:Loans:*' },
    { name: 'funding_account_id', type: 'TEXT', required: true, invariant: 'type = asset' },
    { name: 'interest_account_id', type: 'TEXT', required: true, invariant: 'type = expense', description: '상환의 이자 절반이 기표될 곳' },
    { name: 'principal_minor', type: 'INTEGER', required: true, unit: 'minor units', invariant: 'sum(행 원금) = 이 값' },
    { name: 'annual_rate_text', type: 'TEXT', required: true, unit: 'decimal string %', description: '"4.2" = 4.2%. float 아님 — 360번 복리로 굴린다' },
    { name: 'method', type: 'TEXT', required: true, invariant: 'annuity|equal_principal|bullet|interest_only' },
    { name: 'payment_day', type: 'INTEGER', required: true, unit: '1-31 or -1', description: '-1이면 말일. 2월엔 clamp' },
  ]}
/>

## 균형이 정확한 이유

₩1,000,000을 보내 $750.00을 받으면 내재환율이 1333.3333…으로 안 끝난다. 환율을
저장해 되곱하면 ₩999,998이 나온다. 그럼 허용오차를 발명하게 되고, 그 허용오차가
잡고 싶은 크기(누락된 ₩50 수수료)를 가린다.

그래서 상대금액을 **사실로** 저장한다:

<LedgerExample
  date="2026-07-17"
  narration="Wise 송금 — 양쪽 다 관측"
  legs={[
    { account: 'Assets:Bank:KB:Checking', units: '-1,000,000 KRW', weight: -1000000, source: 'identity' },
    { account: 'Assets:Bank:Wise:USD', units: '750.00 USD', weight: 1000000, source: 'actual', note: '내재환율 1333.3333… — 기록은 하되 균형의 근거가 아님' },
  ]}
/>

USD→JPY처럼 양쪽에 KRW가 없으면 두 환율은 상쇄되지 않는다. 남는 잔차는
**에러가 아니라 FX 스프레드**다. 손익계산서에 올린다:

<LedgerExample
  date="2026-07-17"
  narration="Wise USD→JPY"
  legs={[
    { account: 'Assets:Bank:Wise:USD', units: '-1,000.00 USD', weight: -1300000, source: 'rate' },
    { account: 'Assets:Bank:Wise:JPY', units: '150,000 JPY', weight: 1300500, source: 'rate' },
    { account: 'Expenses:FX:Conversion', units: '-500 KRW', weight: -500, source: 'plug', note: '스프레드. 환율 안에 숨기지 않고 드러낸다' },
  ]}
/>

## 재평가

`units_minor = 0`이면서 weight만 0이 아닌 posting이 가능하다. 외화 잔고는 그대로
두고 KRW 장부가액만 옮긴다 — **weight를 환율에서 끌어내지 않고 따로 저장했기
때문에** 가능한 연산이다.

<LedgerExample
  date="2026-07-31"
  narration="월말 재평가 — USD 강세"
  legs={[
    { account: 'Assets:Bank:Wise:USD', units: '0 USD', weight: 100000, source: 'actual', note: '잔고는 그대로, 장부가액만 +₩100,000' },
    { account: 'Income:FX:Unrealized', units: '-100,000 KRW', weight: -100000, source: 'identity' },
  ]}
/>
